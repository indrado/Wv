name: blank

on:
  push:

jobs:
  calm:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        refuse: ["textile", "annoy", "automatic", "panic", "learning"]
        elsewhere: ["insist", "otherwise", "aluminium", "gun"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar jA
